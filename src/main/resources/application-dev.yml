server:
  port: 8600
spring:
  application:
    name: user-service
  config:
    activate:
      on-profile: dev
    import: optional:configserver:http://config-service:8888

management:
  endpoints:
    web:
      exposure:
        include:
          - "refresh"
          - "bus-refresh"
